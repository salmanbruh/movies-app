import 'package:flutter/material.dart';

class ViewAllList extends StatelessWidget {
  const ViewAllList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
