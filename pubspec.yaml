name: movies_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Features
  home:
    path: features/home
  movies_list:
    path: features/movies_list
  favorites_list:
    path: features/favorites_list
  nav_drawer:
    path: features/nav_drawer

  # Libraries
  core:
    path: libraries/core
  dependencies:
    path: libraries/dependencies
  shared:
    path: libraries/shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
